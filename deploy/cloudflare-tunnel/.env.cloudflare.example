# Build-time browser API host after the Cloudflare DNS records are active.
NEXT_PUBLIC_API_BASE_URL=https://api.sz-yingjin.top

# Permit only the employee-facing Cloudflare origin. Keep the existing LAN
# origin as long as LAN access remains enabled.
CORS_ORIGINS=https://dashboard.sz-yingjin.top,http://192.168.1.77:3100
